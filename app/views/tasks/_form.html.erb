<%= form_for(task) do |f| %>
<% if task.errors.amy? %>

    <div id="error_explanation">
        <ul>
 <% task.errors.full_tasks.each do |task| %>
         <li>
<%= task %>
         </li>   
<% end %>
        </ul>
    </div>
    
<% end %>

<%= f.input :status, 'ステイタス' %>

<%= f.text_field :status %>


  
<%= f.label :content, 'メッセージ' %>

  
<%= f.text_field :content %>


  
<%= f.submit '投稿' %>

<% end %>